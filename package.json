{
  "name": "stream_window",
  "version": "0.1.0",
  "private": true,
  "main": "build/main.js",
  "dependencies": {
    "cors": "^2.8.5",
    "cra-template": "1.2.0",
    "electron": "^34.0.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.5",
    "react-scripts": "5.0.1",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "scripts": {
    "start": "set PORT=4000 && react-scripts start",
    "electron": "electron .",
    "build": "react-scripts build && copy src\\portal.js build\\static\\js\\portal.js && copy key.pem build\\key.pem && copy cert.pem build\\cert.pem",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
